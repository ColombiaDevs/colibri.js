/**
 * @fileoverview Template parser for Colibrí.js
 *
 * This module should parse HTML templates into an Abstract Syntax Tree (AST).
 */

/**
 * Parses an HTML template string into an AST
 * @param {string} template - HTML template to parse
 * @returns {Object} Root AST node
 */
export function parse(template) {
  // TODO: Implement HTML template parser
  console.warn("Template parser needs implementation");

  return {
    type: "root",
    children: [],
  };
}
